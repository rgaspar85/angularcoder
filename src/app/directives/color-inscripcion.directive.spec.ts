import { ColorInscripcionDirective } from './color-inscripcion.directive';

describe('ColorInscripcionDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorInscripcionDirective();
    expect(directive).toBeTruthy();
  });
});
